# ONFERENCE — 국제 컨퍼런스 해외 연사 초청자 관리 대시보드

국제행사에서 **해외 연사를 초청할 때 발생하는 업무 전 주기**(발굴 → 초청·계약 → 출입국·사증 →
여행·의전 → 콘텐츠 → 현장 운영 → 정산·사후)를 하나의 관리 대시보드로 관리하기 위한 **내부 전용** 도구다.
배포하지 않고 사내에서 로컬로 구동해 사용한다.

## 실행

### 1) 단일 HTML 파일 (권장 · 설치 불필요)

`onference-dashboard.html` **한 파일만 내려받아 브라우저로 열면** 바로 동작한다.
서버·설치·인터넷 연결이 필요 없고, 사내 공유 폴더나 USB에 두고 그대로 사용할 수 있다.

```
onference-dashboard.html   ← 더블클릭 (Chrome / Edge / Safari)
```

- CSS·JS·데이터가 모두 이 파일 안에 인라인되어 있어 외부 의존성이 없다 (약 400KB).
- 입력한 내용은 **연 브라우저의 localStorage** 에 저장된다(파일 자체는 변경되지 않음).
  따라서 같은 PC·같은 브라우저에서 이어서 작업할 수 있고, 다른 사람과 자동 동기화되지는 않는다.
- 소스를 수정한 뒤에는 `npm run build:html` 로 이 파일을 다시 생성한다.

### 2) 개발 모드 (소스 수정 시)

```bash
npm install
npm run dev         # http://localhost:5173
npm run build       # 타입 체크 + 정적 빌드(dist)
npm run preview     # 빌드 결과 미리보기
npm run build:html  # 단일 HTML 파일(onference-dashboard.html) 재생성
```

- 서버·DB 없이 브라우저에서 동작하며, 상태는 `localStorage` 에 저장된다.
- 처음 실행하면 데모 행사(제12회 국제 스마트시티·AI 컨퍼런스, 해외 연사 17명)가 시드로 로드된다.
- `행사 설정·템플릿 > 데모 데이터 초기화` 로 언제든 초기 상태로 되돌릴 수 있다.

## 화면

| 그룹 | 화면 | 핵심 내용 |
|---|---|---|
| 현황 | 대시보드 | KPI 5종, 초청 퍼널, 비자 도넛, 체류 간트, 단계별 업무 진행, 마일스톤 |
| 현황 | 리스크·경보 | D-day 역산 기반 8종 리스크 자동 판정 및 리포트 |
| 초청 관리 | 초청 파이프라인 | 드래그 칸반(6단계+거절), 대체 후보 풀, 다양성 점검 |
| 초청 관리 | 연사·초청자 | 목록/검색/Excel 내보내기, 상세 7개 탭, 개인 일정표 인쇄 |
| 초청 관리 | 세션·프로그램 | 일자별 프로그램, 연사 배정, 통역·녹화 |
| 실무 진행 | 출입국·비자 | 사증 종류 판정, 인정서(CCVI), K-ETA, 여유일 역산 보드 |
| 실무 진행 | 여행·의전 | 입국 일정, 항공 여정, 루밍 리스트, 배차, 의전 요구사항 |
| 실무 진행 | 제출물·콘텐츠 | 연사×13개 항목 매트릭스, 기한 경과 리마인드 |
| 실무 진행 | 현장 운영 | 현장 상태 보드, 일자별 런시트, 비상 연락망, 온라인 연사 |
| 정산·사후 | 정산·예산 | 강연료·원천징수·조세조약·해외송금·실비, 예산 집행 |
| 협업 | 업무·체크리스트 | 템플릿 자동 생성 업무(연사별), 단계·담당 필터 |
| 협업 | 커뮤니케이션 | 메일 템플릿 8종, 발송 이력, 팔로업 |
| 협업 | 변경 이력 | 전 엔터티 감사 로그 검색·내보내기 |
| 설정 | 행사 설정·템플릿 | 행사 정보, 지원 패키지, 체크리스트 템플릿, 권한 |

## 설계 문서

- [`docs/01_기능명세서.md`](docs/01_기능명세서.md) — 화면·기능 ID 체계 기반 기능 명세
- [`docs/02_업무프로세스.md`](docs/02_업무프로세스.md) — 8단계 프로세스, RACI, D-day 역산 일정표
- [`docs/03_데이터모델.md`](docs/03_데이터모델.md) — 엔터티·상태 코드·파생 로직
- [`docs/04_리서치노트.md`](docs/04_리서치노트.md) — 해외 연사 초청 부가 업무 리서치 및 확장 백로그

## 기술 구성

- React 19 + TypeScript + Vite (의존성 최소화 — 라우터·차트·UI 라이브러리 없음)
- 해시 라우터(`src/lib/router.ts`), Context + Reducer 상태관리(`src/store.tsx`)
- 차트는 SVG/CSS 직접 구현(`src/components/ui.tsx`) — 색상은 색각이상(CVD) 검증을 통과한 팔레트 사용,
  모든 값은 직접 라벨로 표기하여 색만으로 의미를 전달하지 않음
- 라이트/다크 테마 토큰(`src/styles.css`), 인쇄 레이아웃(개인 일정표·런시트) 지원

```
onference-dashboard.html  단일 파일 배포본(빌드 산출물)
scripts/build-html.mjs    단일 HTML 빌드 스크립트(JS·CSS 인라인)
src/
  types.ts          도메인 모델
  data/seed.ts      데모 시드(행사·연사·세션·업무·템플릿·이력)
  lib/              format · metrics(리스크/준비도/KPI) · csv · router
  components/ui.tsx 카드·배지·테이블·도넛·퍼널·간트 등 공통 UI
  pages/            14개 화면
```

## 주의

- 데모 데이터의 인물·기관명은 모두 가상이다.
- 사증·세무 관련 화면의 기준값(심사 기간, 원천징수율 등)은 내부 운영 참고용이며,
  실제 집행 전 관계 기관·세무 대리인의 최신 안내를 확인해야 한다.
- 여권번호·계좌번호는 화면에서 마스킹되며, 원본 문서는 별도 문서고에서 관리하는 것을 전제로 한다.
